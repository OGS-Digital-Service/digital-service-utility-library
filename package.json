{
  "scripts": {
    "start": "npx @11ty/eleventy --serve & npx tailwindcss -i tailwind-setup.css -o css/tailwind.css --minify --watch",
    "11ty": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "test": "pa11y-ci",
    "pagefind": "npx -y pagefind --site _site  --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "pa11y-ci-reporter-html": "^5.0.2",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "@nysds/components": "^1.18.3",
    "@nysds/styles": "^1.18.3",
    "pagefind": "^1.3.0"
  }
}
